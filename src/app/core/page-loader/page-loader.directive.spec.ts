import { PageLoaderDirective } from './page-loader.directive';

describe('PageLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new PageLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
